name: Build Polaris Controller Jar

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11

      - name: Cache Gradle packages
        uses: actions/cache@v2
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Run Gradle tests
        run: ./gradlew polaris-controller:test

      - name: Build with Gradle
        run: ./gradlew polaris-controller:build

#      - uses: actions/upload-artifact@v2
#        with:
#          name: polaris.jar
#          path: polaris-controller/build/libs/polaris.jar
